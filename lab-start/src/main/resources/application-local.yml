# src/main/resources/application-local.yml

server:
  port: 8081

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: "jdbc:mysql://10.33.9.41:3306/lab_platform_local?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=Asia/Shanghai&allowPublicKeyRetrieval=true"
    username: root
    password: zZ15903750100.

  flyway:
    enabled: true # 本地启动时自动跑 SQL

mybatis-plus:
  configuration:
    # ✅ 开启 SQL 打印
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl

usr:
  env: "local"

lab:
  jwt:
    # 随便写一串乱码，但在生产环境要保密且足够长
    secret: ${LAB_JWT_SECRET:LocalDevSecretKeyOnlyForDebuggingDO_NOT_USE_IN_PROD}
    expiration: 86400000 # 24小时 (单位毫秒)
    token-header: Authorization
    token-prefix: "Bearer "