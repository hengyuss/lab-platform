spring:
  datasource:
    driver-class-name: org.h2.Driver
    # MODE=MySQL 模拟 MySQL 语法
    url: jdbc:h2:mem:testdb;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
  flyway:
    enabled: true # 让 Flyway 在 H2 里自动建