<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.hengyu.lab.system.permission.infrastructure.mapper.RoleMapper">
  <resultMap id="RolePOResult" type="RolePO">
    <id property="roleId"               column="role_id"/>
    <result property="roleName"         column="role_name"/>
    <result property="roleKey"          column="role_key"/>
    <result property="roleSort"         column="role_sort"/>
    <result property="dataScope"        column="data_scope"/>
    <result property="status"           column="status"/>
    <result property="menuCheckStrictly" column="menu_check_strictly"/>
    <result property="createTime"       column="create_time"/>
    <result property="updateTime"       column="update_time"/>
    <result property="deleted"          column="deleted"/>
  </resultMap>
  
  <sql id="selectRolePO">
    select role_id, role_name, role_key, role_sort, data_scope, status, menu_check_strictly, create_time, update_time, deleted
    from sys_role r
  </sql>
  
  <select id="listRole" parameterType="RolePO" resultMap="RolePOResult">
    <include refid="selectRolePO"></include>
    where r.deleted= '0'
    <if test="roleId != null and roleId != 0">
      AND r.role_id = #{roleId}
    </if>
    <if test="roleName != null and roleName != ''">
      AND r.role_name like concat('%', #{roleName}, '%')
    </if>
    <if test="status != null and status != ''">
      AND r.status = #{status}
    </if>
    <if test="roleKey != null and roleKey != ''">
      AND r.role_key like concat('%', #{roleKey}, '%')
    </if>
    order by r.role_sort
  </select>


</mapper>
